{
  "dependencies": {
    "cypress": "^10.11.0",
    "docker": "^0.2.14"
  },
  "scripts": {
    "test": "npx cypress run",
    "testEnv": "CYPRESS_HOST=http://localhost npx cypress open"
  },
  "devDependencies": {
    "cypress-dotenv": "^2.0.0",
    "dotenv": "^16.0.3"
  }
}
